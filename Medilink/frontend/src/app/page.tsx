/**
 * Placeholder root route — exists only so the App Router has an entry point and the
 * dev server runs. NOT a product screen; replaced when UI work begins.
 */
export default function Home() {
  return (
    <main style={{ padding: 24, fontFamily: "var(--font-manrope), system-ui" }}>
      <h1>MediLink</h1>
      <p>Foundation ready. UI screens not yet implemented.</p>
    </main>
  );
}
