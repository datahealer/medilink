# supabase/ — reused HAMS project (single source of truth)

This holds the **existing** Supabase project's `config.toml`, `migrations/`, and `functions/`,
copied **unchanged** from HAMS in Step 3 (additive migrations only after that).
The remote project is reused as-is (same URL / keys). Do not fork the schema.
